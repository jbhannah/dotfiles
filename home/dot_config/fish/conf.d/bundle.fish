if status is-interactive; and command -sq bundle
    abbr -a -g rbb bundle
    abbr -a -g rbbA bundle add
    abbr -a -g rbbe bundle exec
    abbr -a -g rbbI bundle install
    abbr -a -g rbbU bundle update
end
